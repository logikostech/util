language: php

php:
  - 5.6
  - 7.0
  - 7.1
  - hhvm

cache:
  directories:
    - vendor
    - ~/.composer/cache

before_install:
  - composer install --prefer-source --no-interaction --ignore-platform-reqs

script:
  - vendor/bin/phpunit tests/

notifications:
email: false